import type { ModuleConf } from '../../module/schema/module-conf'

const conf: ModuleConf = {
  name: 'point',
  title: 'Point',
  description: 'Module for managing user points and point-based member groups.',
  category: 'member',
  noSkin: false,
}

export default conf
