import type { ModuleConf } from '../../module/schema/module-conf'

const conf: ModuleConf = {
  name: 'poll',
  title: 'Poll',
  description: 'Module for creating and managing polls.',
  category: 'content',
  noSkin: false,
}

export default conf
