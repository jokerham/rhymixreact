import type { ModuleConf } from '../../module/schema/module-conf'

const conf: ModuleConf = {
  name: 'board',
  title: 'Board',
  description: 'In charge of function of the board. Also includes administrator functions such as creating/managing boards.',
  category: 'service',
  noSkin: false,
}

export default conf
