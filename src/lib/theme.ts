import { createTheme } from '@mui/material/styles'

const FONT_FAMILY = "'Nanum Gothic','Segoe UI','Malgun Gothic',AppleGothic,sans-serif"

export const theme = createTheme({
  typography: {
    fontFamily: FONT_FAMILY,
  },
})
